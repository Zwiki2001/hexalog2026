name: logistics_scanner
description: Logistics Barcode Scanner - Warehouse Management App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  mobile_scanner: ^5.2.3
  googleapis: ^12.0.0
  googleapis_auth: ^1.6.0
  http: ^1.2.0
  intl: ^0.19.0
  vibration: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/service_account.json
